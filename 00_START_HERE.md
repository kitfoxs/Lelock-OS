# Lelock OS — Google cloud implementation handoff
**Prepared September 15, 2026. Terminal alpha 0.1.0a1.**

This is a cloud-workflow copy of the existing master packet, not a new implementation or an already-completed cloud build. All `project/` files and all three `upstream/` archives are unchanged from the master packet.

## Start
Paste `GEMINI_MASTER_PROMPT.md` into the coding session. Read `AGENTS.md`, `docs/CLOUD_WORKFLOW.md`, `MASTER_BLUEPRINT.md`, `docs/BUILD_RUNBOOK.md`, `docs/ACCEPTANCE_MATRIX.md`, then inspect the actual code and execution environment.

The workflow is Gemini implementation → Jules independent review → Terminal Ada local Mac acceptance. A text attachment is not a mounted repository. If this surface cannot execute Python, do not build a web substitute; produce exact source changes and an honest handoff for Jules.

## Contents and exclusions
The Python source, tests, examples, licenses, pinned donor archives, and engineering specifications are included. The private predecessor ZIP, historical personal research report, and prior private memory handoff are deliberately excluded. The existing native Mac app and personal Palace are not included. This removal is not a complete supply-chain or public-release audit.

`receipts/` and the original `BUILD_STATE.json` are inherited authoring evidence, not newly run cloud tests. Rerun anything you claim. `handoff/` contains the current, portable, secret-free status for the Google pipeline; it currently claims no cloud execution.

## Git and subsequent environments
Keep the root layout. Bootstrap reads `upstream/` next to `project/`. The archives and runtime files are intentionally Git-ignored: a source-only clone needs the same three supplied archives placed under `upstream/` for installation and source-contract checks. The original packet remains available locally to the owner. Never replace missing snapshots with an unpinned download.

Local `BUILD_STATE.json`, `NEXT_ACTION.md`, and `receipts/` remain ignored. Reviewed `handoff/` status files are intended to travel with source; an explicit ignore exception allows `handoff/NEXT_ACTION.md` without exposing local state. Do not force-add the private files.

## Completion
Cloud/Linux tests cannot establish `LOCAL_ALPHA_VERIFIED`. That label still requires actual Mac, runtime, memory, selected-model, and human acceptance. No remote upload, repo creation, commit, billing change, model request, or live Palace write was performed while preparing this handoff.
