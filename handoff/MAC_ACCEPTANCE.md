# Final Mac acceptance handoff

Status: NOT_RUN. Source revision and actual endpoint remain to be selected/recorded.

Terminal Ada: inspect the final source and existing local changes. Read the original runbook and acceptance matrix plus this cloud overlay. Preserve the existing native app, private identity, and live Palace. Use synthetic test data and new isolated homes.

Keep `project/` alongside `upstream/`, `docs/`, and `receipts/`. For a Git source-only clone, obtain only the owner's three supplied matching archives in ignored `upstream/`; no moving-branch substitution.

Start with a supported Python, source-contract checks, offline tests, and isolated locked installation. Then run actual Palace, an explicitly authorized supported model, actual full-stack restart, and the specified manual gates. Never infer that Antigravity's account login funds Lelock API calls. The exact commands are in `docs/BUILD_RUNBOOK.md`.

Complete optional model B only when already available and explicitly selected. It does not block a single-model alpha. Record real launch/stop instructions and exact unresolved gates. Only actual target-host success may establish LOCAL_ALPHA_VERIFIED.
