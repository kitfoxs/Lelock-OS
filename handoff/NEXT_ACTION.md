# Next action: recover current application source

Publication repair is not a new application build. The current `project/src/lelock/`
from the maintainer Mac is missing in Git and was not available to this repair session.
Use `docs/SOURCE_RECOVERY.md`; do not reconstruct it from older cloud artifacts.

After a reviewed source commit, run the publication checker and real application suite
in a fresh checkout. Then provide exact public upstream archives, test the installer,
and verify the recovered bridge against the repaired SillyTavern imports. Keep Mac,
model, browser and human acceptance separate. Preserve all recent companion/lore work.

The README's old 102-test badge was a claim without source in the published tree;
`docs/history/LOVABLE_STATUS.json` is an older cloud record, not evidence of Jules or
of the newer Mac run. Replace neither with invented successful results.
