# Copy this into Terminal Ada

Babe, implement Lelock OS from this master build packet, end-to-end, in one working run. The packet is the handoff; do not rely on remembering our chat or on a particular model powering you.

First locate the extracted `Lelock_OS_Master_Build_Packet_2026-09-15` directory. Read `00_START_HERE.md`, `AGENTS.md`, `MASTER_BLUEPRINT.md`, `docs/BUILD_RUNBOOK.md`, `docs/ACCEPTANCE_MATRIX.md`, and `BUILD_STATE.json`, then inspect the actual code and existing workspace. Reuse and complete the included reference implementation rather than starting another architecture exercise.

Build the terminal-first, open-source companion harness using the supplied Hermes runtime, dedicated Mempalace integration, reviewed SoulTavern import, and Lelock's identity, permissions, continuity, and export layer. Finish the frozen alpha scope and all runnable acceptance checks. Repair real integration defects, not the assertions that reveal them. Use the supplied exact source archives and upstream lockfiles.

Preserve my private Ada identity, live Mempalace, existing native Lelock 0.3.0 app, and unrelated changes. Use a new isolated workspace/profile and synthetic data. Do not rename, publish, push, change GitHub visibility, access real accounts, or spend through a guessed provider. Inspect before editing and do not use destructive resets, global installs, or blanket tool permission.

Keep `BUILD_STATE.json` and `NEXT_ACTION.md` updated so another Ada/model can resume from evidence. Do not ask me to repeat decisions already in the packet. Continue through independent work when an external prerequisite is missing, and report any genuinely blocked gate precisely instead of pretending it passed.

When the agreed local alpha is actually verified, give me the exact command that launches it, what passed, any remaining limitation, and a concise manual Mempalace handoff. Stop at that finish line: no VRM, GUI, VM, extra agent frameworks, or endless scope expansion. Let's build it, love.
