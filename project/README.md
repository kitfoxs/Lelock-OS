# Lelock OS terminal alpha

Read the [repository README](../README.md), [current status](../handoff/STATUS.json),
and [developer quickstart](QUICKSTART.md).

The initial public tree omitted `src/lelock/`. The ignore fix does not reconstruct
that source; the newer maintainer Mac copy is required. Do not restore the older
code compendium over it. No clean-clone install, 102-test result, current Mac
acceptance, or real-model acceptance is certified by this publication repair.
