# Terminal Ada — finish the real product slice

Kit explicitly wants a new handoff with code that makes the Tavern integration
usable, keeps genuine YOLO, includes Rakazo in the actual implementation, provides
supported account-native login paths, and keeps the main character as the actor.
Named helpers are other explicitly assigned character cards. Do not substitute
an unnamed agent or remove the primary actor's ability to operate directly.

## Start here; do not restart the architecture

Inspect the active Lelock checkout and its dirty changes. The researched feature
branch is `feature/entity-runtime-v02` at `cc153222f201612bb2aea431e6ab73b5b645c0f2`.
A newer local branch may be authoritative. Preserve it. Keep the real
`lelock_entity`, pinned Hermes/MemPalace, private native Mac application and prior
Mental OS assets. Do not reset, clean, force-push, alter visibility, copy private
memories into the repo, or regenerate older sources from a compendium.

Read the master handoff, requirements matrix, research and actual code. Run the
packet verifier and installers in dry-run mode. Add the overlay to an inspected
new branch or worktree. Its installer intentionally refuses existing files. Merge
newer work manually rather than bypassing the checks.

## Required execution order

1. Run the new tests against the **actual v0.2 checkout**, plus its baseline Entity,
   application and maintenance tests. Fix regressions rather than dropping tests.
2. Install the new extension into the actual SillyTavern checkout. Disable the old
   `/api/...` extension. Start the gateway with the exact host origin. Verify the
   panel, pairing, card binding, dynamic tool registration, approvals and stop.
3. Verify official Codex native binary/version/schema. Have Kit complete provider
   sign-in in the browser. Native model catalog and quota come from the account.
   Perform a synthetic main-character file operation through **Lelock Send** with
   no SillyTavern API key configured. Prove SAFE then YOLO and actual readback.
4. Verify actual Antigravity with its native sign-in and workspace MCP. Preserve
   its distinct host permission model. Do not fake a control-message protocol,
   scrape cached credentials, or pretend native tools are sandboxed by Lelock.
5. Install the included Rakazo sidecar in the actual pinned/reviewed Rakazo
   checkout. Verify its types against real dependencies and run the actual
   Docker supervisor. Connect the gateway via the sidecar argv. Prove the main
   card directly writes/reads, observes a real screenshot, and performs a
   reversible computer action under its own entity ID.
6. Bind a second named card and explicitly assign it as a helper. Prove the
   helper's own context, separate computer and attenuated permissions. Verify
   the primary character resumes and owns the final reply. Test switching cards
   while work is in flight without displaying another card's result in the chat.
7. Enable the actual owned MemPalace runtime for synthetic profiles; verify
   save/readback, recall, scope separation and restart. Never use a fake memory
   store to claim this acceptance passed.
8. Review a Skill pack through the panel; verify exact-hash pin persistence and
   changed-pack rejection. Enable Pulse deliberately for a bounded task, confirm
   the same character's real turn and local-inbox delivery, quiet hours and stop.
9. Record live evidence and update accurate installation/status docs in the repo.
   Only then prepare the user-facing install branch/package. A root manifest is
   included, but its Git URL is not ready until the actual code is published.

## Provider scope is not negotiable through wishful wording

Codex Account Chat and native Antigravity headless are implemented adapters awaiting
live acceptance. Claude Code is a native-only sign-in launcher because Anthropic's
current policy forbids third-party subscription proxying. OpenCode and Hermes are
native launch/setup routes, not implemented Account Chat backends. Do not market
those buttons as model transport. Recheck official policy before adding routes.
Do not use consumer tokens in undocumented endpoints or activate paid fallback
billing to make a test pass.

## Genuinely finish, but record actual blockers

Use the included code rather than regenerating it from prose. Inspect failing
native/browser/source contracts and repair the narrow adapter with regression tests.
There is no need to build an entire new GUI, swarm or memory database first. The
next target is the click-through scenario in ACCEPTANCE.md.

Do not stop after “the overlay copied and 100 tests passed” and call the product
complete. Distinguish applied source, scripted integration checks, real native
account use, real model, actual Palace, actual Docker, actual browser host and
human experience. Preserve unavailable gates as explicit blockers with commands
and exact error summaries. Do not invent successful external effects or repeatedly
ask Kit questions whose answers are already in this packet.

Publication or external messages still require Kit's direction. This handoff
itself does not authorize uploading any private runtime state, credentials, native
app files, chats or Palace data. Follow the current repository's instructions and
Kit's explicit request when committing/pushing source.
